/* eslint-disable prettier/prettier */

export const jwtConstants = {
accessTokenSecret: 'accessTokenSecretKey', // Use a strong secret key in production
  refreshTokenSecret: 'refreshTokenSecretKey', // Use a strong secret key in production
};
